# Core Application Configuration
spring.application.name=user-management-service
server.port=6075
server.servlet.context-path=/

# Database Configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=jxgEQeXHuPq8VdbyYFNkANdudQ53YUn4
jwt.expiration=3600000000
jwt.reset.expiration=900000

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=virgile405@gmail.com
spring.mail.password=raix vgyb cgpl sdbn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# OTP Configuration
otp.max-attempts=10
otp.expiry.minutes=10
otp.length=6
otp.attempt-window-hours=24

# Eureka Configuration
eureka.client.serviceUrl.defaultZone=http://eureka:password@localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# OpenAPI/Swagger Configuration
openapi.service.title=authentication-service
openapi.service.version=1.0.0
openapi.service.url=http://localhost:8084
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
spring.cloud.compatibility-verifier.enabled=false

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://virgilendayambaje:N7TafWJ1dnFTu95Y@cluster0.zr9sn.mongodb.net/commentdb

# Redis Configuration
#spring.redis.host=redis
#spring.redis.port=6890

# Kafka Configuration
spring.kafka.bootstrap-servers=kafka:29092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.group-id=user-management-service-group

# Microservices Database URLs
audit.compliance.db.url=jdbc:postgresql://postgres:5432/audit_compliance_db
role.access.db.url=jdbc:postgresql://postgres:5432/roleaccess_db
sop.workflow.db.url=jdbc:postgresql://postgres:5432/sop_work_flow_db
user.management.db.url=jdbc:postgresql://postgres:5432/sop_db

# Logging Configuration
logging.level.root=INFO
logging.level.com.user_management_service=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Server Compression
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000

# Cache Configuration
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterAccess=600s

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Session Configuration
server.servlet.session.timeout=30m

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never
server.error.include-exception=false

#GRPC
grpc.client.role-access-control-service.negotiation-type=plaintext